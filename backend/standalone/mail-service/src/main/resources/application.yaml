spring:
    application:
        name: mail-service
    profiles:
        active: ${SPRING_PROFILES_ACTIVE:dev}

server:
    port: ${SERVER_PORT:8082}

# Swagger UI config
springdoc:
    api-docs:
        enabled: true
    swagger-ui:
        enabled: true
        path: /swagger-ui.html
